[package]
name = "sod-ex"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4", features = ["derive"] }
dirs = "6.0.0"
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"
